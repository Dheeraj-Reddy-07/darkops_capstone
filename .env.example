# Public variables (exposed to browser via Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Set for mobile or external backend (e.g., http://10.0.2.2:5000 for Android emulator or http://<your-ip>:5000 for physical phone)
VITE_API_BASE_URL=

# Server-only variables (never exposed to browser)
SUPABASE_SERVICE_ROLE_KEY=
